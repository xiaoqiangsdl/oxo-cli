#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', 'Generate a new project from a template')
  .command('list', 'List available official templates')
  .command('add', 'Add a new custom templete')
  .parse(process.argv)
